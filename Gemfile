source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in fac3_logging.gemspec
gemspec

# gem 'lograge'
# gem 'remote_syslog_logger'